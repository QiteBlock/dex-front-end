const qiteAddress = require("./qite-address.json")
const qiteSwapAbi = require("./qite-swap-abi.json")
const erc20Abi = require("./erc20-abi.json")
const qitePoolAbi = require("./qite-pool-abi.json")

module.exports = {
    qiteAddress,
    qiteSwapAbi,
    erc20Abi,
    qitePoolAbi,
}
